name: Test — Reusable Hello v7

on:
  workflow_call:
    inputs:
      service-dir:
        required: true
        type: string
    secrets:
      SONAR_TOKEN:
        required: true

jobs:
  test:
    name: Tests & SonarQube
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4
      - run: echo "done in ${{ inputs.service-dir }}"
